// Constante geralmente são declaradas em letras maiúsculas.
// As constantes não são manípulaveis.
// As variáveis são manípulaveis.

const PRECO = 2;
var desconto = 0.2;
var total = PRECO - desconto;
